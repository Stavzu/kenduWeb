export enum HelpingPeopleType {
    GRADUATE = "graduate",
    ORGANIZER = "organizer",
    LECTOR = "lector",
    PARTNER = "partner",
}

export enum LinkType {
    PERSONAL = "personal",
    SOCIAL = "social",
    CONTACT = "contact",
    PAGE = "page",
}

export enum PartnerType {
    GOLD = "gold",
    SILVER = "silver",
    COMMUNITY = "community",
}
